[package]
name = "dirtree-rdm"
version = "0.1.0"
edition = "2021"
description = "CRUD CLI for dirtree roadmap nodes with BNF-validated README.md mutations"

[[bin]]
name = "dirtree-rdm"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
regex = "1"
anyhow = "1"
